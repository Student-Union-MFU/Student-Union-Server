-- ความเห็นต่อฐาน: เพิ่มข้อ "พื้นที่" แยกจาก "วิว"
--
-- แอปย้ายไปถามเฉพาะสิ่งที่ตอบได้ตอนยืนอยู่ที่ฐาน — วิวกับพื้นที่ — และย้ายข้อ "กิจกรรม"
-- ไปถามทีเดียวตอนจบเส้นทาง (ดู 000033) เพราะกิจกรรมของฐานเดียวเทียบกับอะไรไม่ได้จนกว่า
-- จะเดินครบ
--
-- สองข้อนี้แยกกันจริง ไม่ใช่คำเดียวกันคนละคำ: วิวคือสิ่งที่มองออกไปเห็น พื้นที่คือสิ่งที่
-- ยืนอยู่ในนั้น — ร่มเงา ที่นั่ง ที่ว่างพอให้ทั้งกลุ่มยืนไหม เป็นคนละเรื่องและแก้ด้วยคนละวิธี
-- ในปีหน้า ฐานที่วิวสวยแต่ไม่มีร่มจะได้คะแนนสองแบบ ซึ่งเป็นข้อมูลที่ผู้จัดใช้ได้จริง
--
-- NULL ได้ ด้วยเหตุผลเดียวกับ 000031 — แถวที่ตอบก่อน migration นี้ไม่เคยถูกถาม การใส่ค่า
-- ให้ย้อนหลังคือการแต่งคำตอบให้คนที่ไม่ได้ตอบ
--
-- แอปรุ่นที่ส่ง rating_area มาก่อนคอลัมน์นี้จะมีอยู่ ถูก decoder ของ Go ทิ้งไปเงียบ ๆ
-- (ไม่มี DisallowUnknownFields ที่ไหนในโปรเจกต์นี้) จึงปล่อยรุ่นแอปก่อน migration ได้
-- โดยไม่ทำให้ของเดิมพัง — ตั้งใจให้เป็นแบบนั้น เพราะขอให้คนกลางทางอัปเดตแอปไม่ได้
ALTER TABLE checkin_feedback
  ADD COLUMN IF NOT EXISTS rating_area SMALLINT CHECK (rating_area IS NULL OR rating_area BETWEEN 1 AND 5);
